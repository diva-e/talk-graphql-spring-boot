query FetchPerson($personId: ID) {
    person(personId: $personId) {
        id
        firstName
        lastName
        contracts {
            id
            name
            ... on ContractCarInsurance{
                numberPlate
            }
        }
    }
}